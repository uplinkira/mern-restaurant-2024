{
  "name": "mern-restaurant-2024",
  "version": "1.0.0",
  "engines": {
    "node": "20.11.1",
    "npm": "10.2.4"
  },
  "scripts": {
    "start": "cd backend && npm start",
    "heroku-postbuild": "cd client && NODE_ENV=production npm install && npm run build && cd ../backend && npm install",
    "postbuild": "echo 'Build completed with NODE_ENV=' $NODE_ENV"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@babel/plugin-proposal-private-property-in-object": "^7.21.11"
  }
}
